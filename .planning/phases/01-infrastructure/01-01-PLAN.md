---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - requirements-dev.txt
  - pyproject.toml
  - .env.example
  - .gitignore
  - main.py
  - storage/.gitkeep
  - storage/templates/Г39/.gitkeep
  - storage/templates/Г38/.gitkeep
  - storage/contracts/.gitkeep
  - storage/logs/.gitkeep
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "Running `pip install -r requirements.txt` installs all packages without version conflicts"
    - "A `.env.example` file documents every required environment variable with a placeholder value"
    - "The `storage/` directory tree exists with subdirectories for templates (Г39, Г38), contracts, and logs"
    - "Running `python main.py` with no `.env` file exits immediately with a readable error, not a Python traceback"
    - "Ruff can lint the project without configuration errors"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned runtime dependencies"
      contains: "python-telegram-bot==22.7"
    - path: "requirements-dev.txt"
      provides: "Pinned dev/test dependencies"
      contains: "pytest"
    - path: "pyproject.toml"
      provides: "Ruff linter configuration"
      contains: "[tool.ruff]"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "TELEGRAM_BOT_TOKEN="
    - path: "main.py"
      provides: "Bot entry point skeleton"
      contains: "if __name__ == \"__main__\""
    - path: "storage/templates/Г39/.gitkeep"
      provides: "Г39 template directory marker"
    - path: "storage/templates/Г38/.gitkeep"
      provides: "Г38 template directory marker"
  key_links:
    - from: "main.py"
      to: "storage/"
      via: "Path existence check at startup"
      pattern: "storage"
---

<objective>
Create the complete project scaffold: pinned dependencies, linter config, environment variable template, directory tree, and a main.py skeleton that validates the environment at startup.

Purpose: Every subsequent phase builds on this scaffold. Getting dependencies pinned and directories created first eliminates "works on my machine" problems for later plan execution.
Output: Installable Python project with storage directory tree, ruff-lintable code, and a startup entry point that fails fast on missing config.
</objective>

<execution_context>
@C:/Users/User/Documents/rent-contract-bot/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Documents/rent-contract-bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pinned dependency files and linter config</name>
  <files>requirements.txt, requirements-dev.txt, pyproject.toml, .gitignore</files>

  <read_first>
    - .planning/research/STACK.md (exact pinned versions for all packages)
  </read_first>

  <action>
Create `requirements.txt` with these exact pinned versions (no ranges, no `>=`):
```
python-telegram-bot==22.7
anthropic==0.86.0
docxtpl==0.20.2
SQLAlchemy==2.0.48
aiosqlite==0.22.1
pydantic==2.12.5
python-dotenv==1.1.0
httpx==0.28.1
Pillow==11.2.1
```

Create `requirements-dev.txt`:
```
-r requirements.txt
pytest==8.3.5
pytest-asyncio==0.25.3
ruff==0.11.2
```

Create `pyproject.toml`:
```toml
[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```

Create `.gitignore`:
```
__pycache__/
*.py[cod]
.env
*.db
storage/contracts/
storage/logs/
*.pdf
*.docx
!storage/templates/**/*.docx
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
```
  </action>

  <verify>
    <automated>grep "python-telegram-bot==22.7" "C:/Users/User/Documents/rent-contract-bot/requirements.txt" && grep "pytest-asyncio" "C:/Users/User/Documents/rent-contract-bot/requirements-dev.txt" && grep "\[tool.ruff\]" "C:/Users/User/Documents/rent-contract-bot/pyproject.toml"</automated>
  </verify>

  <acceptance_criteria>
    - requirements.txt contains exactly `python-telegram-bot==22.7` (grep-verifiable)
    - requirements.txt contains exactly `anthropic==0.86.0` (grep-verifiable)
    - requirements.txt contains exactly `SQLAlchemy==2.0.48` (grep-verifiable)
    - requirements.txt contains exactly `aiosqlite==0.22.1` (grep-verifiable)
    - requirements.txt contains exactly `pydantic==2.12.5` (grep-verifiable)
    - requirements.txt contains exactly `Pillow==11.2.1` (grep-verifiable)
    - requirements-dev.txt contains `-r requirements.txt` on the first line
    - requirements-dev.txt contains `pytest-asyncio` (grep-verifiable)
    - pyproject.toml contains `[tool.ruff]` section (grep-verifiable)
    - pyproject.toml contains `asyncio_mode = "auto"` (grep-verifiable)
    - .gitignore contains `.env` entry (grep-verifiable)
  </acceptance_criteria>

  <done>All four files exist with correct pinned versions. Ruff can be invoked without errors: `ruff check .` returns exit code 0 (no files yet, no errors).</done>
</task>

<task type="auto">
  <name>Task 2: Create storage directory tree, .env.example, and main.py skeleton</name>
  <files>
    .env.example,
    main.py,
    storage/templates/Г39/.gitkeep,
    storage/templates/Г38/.gitkeep,
    storage/contracts/.gitkeep,
    storage/logs/.gitkeep
  </files>

  <read_first>
    - .planning/ROADMAP.md (Phase 1 success criteria: directory structure required)
    - .planning/research/PITFALLS.md (Pitfall 4: PicklePersistence path; Pitfall 3: storage/logs needed)
  </read_first>

  <action>
Create `.env.example` with all required environment variables:
```
# Telegram Bot Token — get from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Anthropic API Key — get from console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-your_key_here

# Storage paths (relative to project root)
STORAGE_DIR=storage
LOG_LEVEL=INFO
```

Create empty `.gitkeep` files to commit the directory structure:
- `storage/templates/Г39/.gitkeep` (empty file)
- `storage/templates/Г38/.gitkeep` (empty file)
- `storage/contracts/.gitkeep` (empty file)
- `storage/logs/.gitkeep` (empty file)

Create `main.py` as a startup skeleton that:
1. Imports `os` and `pathlib.Path`
2. Attempts to load `.env` using `python-dotenv` before any other imports
3. Validates `TELEGRAM_BOT_TOKEN` and `ANTHROPIC_API_KEY` are set and non-empty — raises `SystemExit` with a human-readable message if either is missing (NOT a traceback)
4. Verifies `storage/templates/Г39` and `storage/templates/Г38` directories exist — raises `SystemExit` if missing
5. Has a `if __name__ == "__main__":` block that prints "Bot starting..." and exits cleanly (no actual bot logic yet — that comes in Phase 5)

Exact structure for main.py:
```python
"""Rent Contract Bot — entry point."""
import sys
from pathlib import Path

from dotenv import load_dotenv

# Load .env before accessing environment variables
load_dotenv()

import os  # noqa: E402 — must be after load_dotenv


def validate_environment() -> None:
    """Validate required environment variables and directory structure."""
    missing = []
    for var in ("TELEGRAM_BOT_TOKEN", "ANTHROPIC_API_KEY"):
        if not os.getenv(var):
            missing.append(var)
    if missing:
        print(
            f"ERROR: Missing required environment variables: {', '.join(missing)}\n"
            f"Copy .env.example to .env and fill in the values.",
            file=sys.stderr,
        )
        sys.exit(1)

    base = Path(__file__).parent / "storage" / "templates"
    for group in ("Г39", "Г38"):
        if not (base / group).is_dir():
            print(
                f"ERROR: Required directory missing: storage/templates/{group}\n"
                f"Run: mkdir -p storage/templates/Г39 storage/templates/Г38",
                file=sys.stderr,
            )
            sys.exit(1)


if __name__ == "__main__":
    validate_environment()
    print("Environment validated. Bot starting...")
    # Bot Application setup goes here in Phase 5
```
  </action>

  <verify>
    <automated>grep "TELEGRAM_BOT_TOKEN" "C:/Users/User/Documents/rent-contract-bot/.env.example" && grep "validate_environment" "C:/Users/User/Documents/rent-contract-bot/main.py" && test -f "C:/Users/User/Documents/rent-contract-bot/storage/templates/Г39/.gitkeep" && test -f "C:/Users/User/Documents/rent-contract-bot/storage/templates/Г38/.gitkeep"</automated>
  </verify>

  <acceptance_criteria>
    - .env.example contains `TELEGRAM_BOT_TOKEN=` (grep-verifiable)
    - .env.example contains `ANTHROPIC_API_KEY=` (grep-verifiable)
    - .env.example contains `LOG_LEVEL=INFO` (grep-verifiable)
    - main.py contains `from dotenv import load_dotenv` (grep-verifiable)
    - main.py contains `load_dotenv()` before `import os` (grep-verifiable)
    - main.py contains `validate_environment` function definition (grep-verifiable)
    - main.py contains `sys.exit(1)` (grep-verifiable, not a traceback on missing env)
    - main.py contains `if __name__ == "__main__":` (grep-verifiable)
    - `storage/templates/Г39/.gitkeep` file exists (test -f verifiable)
    - `storage/templates/Г38/.gitkeep` file exists (test -f verifiable)
    - `storage/contracts/.gitkeep` file exists (test -f verifiable)
    - `storage/logs/.gitkeep` file exists (test -f verifiable)
    - Running `python main.py` without a .env file exits with code 1 and prints "ERROR: Missing required environment variables" to stderr (not a Python traceback)
  </acceptance_criteria>

  <done>Storage directories committed. `.env.example` documents all required variables. `python main.py` without `.env` exits with code 1 and a human-readable error message.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "python-telegram-bot==22.7" requirements.txt` — exits 0
2. `grep "asyncio_mode" pyproject.toml` — exits 0
3. `test -d storage/templates/Г39 && test -d storage/templates/Г38` — exits 0
4. `python main.py 2>&1 | grep "ERROR: Missing required"` — exits 0 (confirms clean fail without traceback)
5. `grep "TELEGRAM_BOT_TOKEN" .env.example` — exits 0
</verification>

<success_criteria>
- All pinned dependencies in requirements.txt (no unpinned ranges)
- .env.example documents TELEGRAM_BOT_TOKEN, ANTHROPIC_API_KEY, STORAGE_DIR, LOG_LEVEL
- storage/templates/Г39/ and storage/templates/Г38/ directories exist and are committed via .gitkeep
- `python main.py` without .env exits code 1 with human-readable error (no traceback)
- ruff check . runs without configuration errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
